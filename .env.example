# Amazon Product Advertising API (PA-API 5.0) — optional.
# Leave these blank to have the Amazon provider report "Not Connected"
# without affecting any other part of the app. Requires an Amazon
# Associates account with qualifying sales.
AMAZON_ACCESS_KEY=
AMAZON_SECRET_KEY=
AMAZON_PARTNER_TAG=
AMAZON_HOST=webservices.amazon.com
AMAZON_REGION=us-east-1
AMAZON_MARKETPLACE=www.amazon.com

# Mercado Libre (PA-API-equivalent: OAuth client_credentials) — optional but
# recommended. As of Mercado Libre's 2023 API changes, /sites/{site}/search
# requires an access token even for public results. Register a free app at
# https://developers.mercadolibre.com (no sales requirement) to get these.
# Leave blank to have the Mercado Libre provider report "Not Connected"
# without affecting any other part of the app.
ML_CLIENT_ID=
ML_CLIENT_SECRET=

# The default Mercado Libre country (used until a user picks one in the
# app) is Mexico (MLM); see src/lib/marketplace/countries.ts for the full
# list of supported countries.
